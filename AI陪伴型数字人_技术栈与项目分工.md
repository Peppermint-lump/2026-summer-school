# 基于跨模态情绪冲突检测的 AI 陪伴型数字人项目方案

## 1. 项目概述
随着多模态大模型的发展，现有模型已经能够同时处理文本、音频和视频信息，并用于用户情绪理解。然而，现有多模态情感系统通常侧重于将不同模态特征直接融合，输出单一的综合情绪结果，较少显式保留各模态的独立判断，也缺乏对文本、语音和视觉情绪不一致现象的专门建模。当用户的语言内容与语调、面部表情或行为状态不一致时，简单的融合结果可能掩盖重要的非语言信号。此外，现实环境中的噪声、遮挡、侧脸和转写错误也会导致不同模态的可靠性存在差异。

本项目构建一个具备跨模态情绪冲突检测能力的AI陪伴型数字人系统。系统分别分析用户的文本字面情绪、声音情绪和视觉情绪，在考虑各模态输入质量与置信度的基础上，检测不同模态之间的一致性和冲突类型，并将结果映射为温和确认、情绪倾听或中性澄清等陪伴策略，最终通过大模型回复、语音合成和Live2D表情动作形成完整的人机交互闭环。


项目总体流程如下：

```text
用户语音与摄像头画面
        ↓
音频、视频按对话轮次采集与同步
        ↓
ASR文本 + 音频情绪 + 视觉情绪
        ↓
跨模态情绪一致性与冲突检测
        ↓
陪伴策略选择
        ↓
大模型生成回复
        ↓
TTS语音 + Live2D嘴型、表情与动作
```

---

## 2. 应用场景介绍

### 2.1 应用场景

本项目面向需要自然人机交流和情绪理解的数字人交互场景，主要包括：

- 日常情绪陪伴与聊天；
- 虚拟主播和 AI VTuber；
- 校园心理关怀的非诊断性辅助交互；
- 独居人群、老年人等用户的日常陪伴；
- 智能客服和数字员工的情绪敏感交互；
- 多模态情感计算和人机交互研究原型。

本项目不将数字人定位为心理治疗或医学诊断系统。情绪识别结果只作为交互策略的辅助证据，不用于断言用户真实心理状态或进行疾病判断。

### 2.2 现有多模态情感智能的问题

#### 1. 过度依赖单一模态

很多系统主要根据用户文本判断情绪。例如，用户说“我没事，我挺好的”，系统可能直接将其识别为积极情绪，而忽略其低沉语气、长时间停顿或消极面部状态。

单一模态容易受到以下因素影响：

- 文本可能包含反讽、委婉表达或情绪掩饰；
- 音频可能受到噪声、麦克风质量和口音影响；
- 视频可能受到光照、遮挡、侧脸和表情个体差异影响。

#### 2. 多模态融合通常只关注最终情绪分类

现有多模态方法往往将文本、音频和视觉信息融合成一个最终标签，例如“积极”“中性”或“消极”，但没有显式分析各模态之间是否存在矛盾。

这会掩盖有价值的情绪信号。例如：

```text
文本：积极
语音：消极
视觉：消极
```

如果系统只输出一个综合标签，就无法明确表达“用户的语言表达与非语言信号不一致”。

#### 3. 缺少模态可靠性判断

现实环境中的输入质量并不稳定：

- 摄像头中可能没有检测到有效人脸；
- 用户可能处于侧脸、遮挡或逆光状态；
- 音频可能过短、过轻或存在背景噪声；
- ASR转写可能出现漏词和错词。

如果系统不判断输入质量，容易将低质量模态当作可靠证据，产生误判。

#### 4. 情绪识别与回复策略之间缺少明确映射

部分系统在检测到悲伤后直接生成安慰回复，或让数字人同步表现悲伤。这种简单映射可能造成以下问题：

- 过度放大用户情绪；
- 对用户进行未经证实的心理推断；
- 使用“你明明很难过”等带有压迫感的表达；
- 数字人表情与陪伴角色定位不一致。

#### 5. 数字人展示与情绪算法常被割裂

多模态情绪识别研究通常只输出分类结果，缺少完整的人机交互闭环；而数字人项目虽然具备语音、表情和动作，却不一定具备可靠的跨模态情绪分析能力。

### 2.3 本项目的贡献点

#### 1. 构建完整的多模态陪伴型数字人闭环

基于 Open-LLM-VTuber，实现从语音输入、摄像头采集、情绪分析、冲突检测、陪伴回复到 Live2D 表达的完整系统，而不是仅实现单独的情绪分类模型。

#### 2. 显式检测跨模态情绪冲突

系统不只生成综合情绪标签，而是分别得到：

- 文本字面情绪；
- 音频情绪；
- 视觉情绪；
- 各模态输入质量与可靠性。

随后显式判断文本、语音和视觉之间是否一致，并识别典型冲突模式，例如：

```text
verbal_positive_behavior_negative
```

即用户语言表达偏积极，但语音和视觉信号偏消极。

#### 3. 引入可靠性加权机制

通过模型置信度和输入质量共同计算模态可靠性：

```text
reliability = confidence × input_quality
```

只有在相关模态证据达到一定可靠性时，系统才判定强冲突；当音视频质量不足时，系统输出 `uncertain` 或 `insufficient_evidence`，避免强行判断。

#### 4. 将情绪冲突映射为陪伴策略

冲突检测结果不直接生成固定回复，而是先映射到陪伴策略，例如：

- `positive_engagement`
- `emotional_validation`
- `gentle_check_in`
- `calm_listening`
- `neutral_clarification`
- `normal_conversation`

再由大模型结合上下文生成自然回复。

#### 5. 区分用户情绪与数字人表达

系统明确区分：

```text
user_emotion
avatar_emotion
```

例如，用户表现悲伤时，数字人不一定同步哭泣，而是采用平静、温和和关切的表达，避免情绪镜像带来的负面交互效果。

#### 6. 采用模块化、可替换的模型接口

音频和视频情绪模型通过统一 Provider 接口接入，默认使用 MiMo-V2.5 API，同时保留 Qwen3-Omni-Flash 作为备用和对照。

后续可以将云 API 替换为部署在实验室服务器上的本地 Omni 模型，而不需要重写上层系统。

---

## 3. 项目实现的功能介绍

### 3.1 实时语音交互

系统通过麦克风接收用户语音，并完成：

- 语音活动检测；
- 用户发言起止时间识别；
- ASR语音转写；
- 对话上下文维护；
- 语音打断；
- 大模型回复；
- TTS语音播放。

### 3.2 Live2D数字人展示

系统使用已经购买的“小蝴蝶”Live2D皮套，并支持：

- 模型加载；
- 纹理与物理效果；
- 自动眨眼；
- TTS嘴型同步；
- 待机动画；
- 表情切换；
- 特殊动作扩展；
- Web或Electron桌面展示。

### 3.3 按对话轮次采集音视频

系统以一次用户发言作为一个 `turn`，为每轮生成唯一 `turn_id`，并同步保存：

```text
turn_id
speech_start_ms
speech_end_ms
audio_path
video_path / frame_paths
transcript
```

这样可以确保音频、视频和文字来自同一时间段。

### 3.4 文本情绪分析

ASR文本单独发送给 GLM API，只根据字面内容判断：

- `positive`
- `neutral`
- `negative`
- `uncertain`

文本情绪分析不得参考音频和视频结果，以保持不同模态判断的相对独立性。

### 3.5 音频情绪分析

用户本轮原始音频发送给 MiMo-V2.5，采用 audio-only 模式，分析：

- 情绪极性；
- 细粒度情绪；
- 唤醒程度；
- 语速、停顿、音量等证据；
- 模型置信度。

同时本地计算音频质量，包括：

- 音频时长；
- RMS能量；
- 静音比例；
- 削波情况；
- 信噪质量。

### 3.6 视觉情绪分析

摄像头在本地持续采集视频，并根据用户发言时间范围截取对应片段。

视频模块完成：

- 摄像头帧缓存；
- 时间戳对齐；
- 视频抽帧或低帧率压缩；
- 人脸有效性检测；
- 光照、遮挡、侧脸和多人情况判断；
- MiMo-V2.5 video-only 情绪分析；
- 视觉质量和可靠性输出。

### 3.7 跨模态情绪冲突检测

系统根据三个模态的标签和可靠性判断情绪一致性，输出：

- 一致积极；
- 一致中性；
- 一致消极；
- 语言积极、行为消极；
- 语言消极、行为积极；
- 音频与视觉不一致；
- 证据不足；
- 无法判断。

典型输出：

```json
{
  "conflict": true,
  "conflict_type": "verbal_positive_behavior_negative",
  "conflict_score": 0.72,
  "strategy": "gentle_check_in"
}
```

### 3.8 陪伴策略与回复生成

冲突检测结果先进入规则式策略路由，再发送给 GLM 生成陪伴回复。

系统要求回复：

- 不断言用户隐瞒情绪；
- 不宣称能够准确读取用户内心；
- 不进行心理诊断；
- 不使用质问式表达；
- 允许用户拒绝继续说明；
- 优先使用确认、倾听和温和询问。

### 3.9 数字人情绪反馈

大模型输出：

```json
{
  "reply": "...",
  "avatar_emotion": "neutral",
  "voice_style": "gentle"
}
```

系统将其映射到：

- TTS语速和语气；
- Live2D表情；
- 嘴型；
- 可选动作。

### 3.10 模型切换与对照实验

系统保留统一接口，可在配置文件中切换：

```yaml
emotion_provider: mimo
fallback_provider: qwen
```

支持比较：

- MiMo-V2.5 与 Qwen3-Omni-Flash；
- 仅文本情绪与三模态情绪；
- 简单多数投票与可靠性加权；
- 联合推理与分模态独立推理。

---

## 4. 项目特色介绍

### 4.1 完整数字人交互闭环

本项目不是单独的情绪分类器，而是将跨模态情绪分析真正接入数字人交互流程，实现“感知—判断—回复—表现”的完整闭环。

### 4.2 重点关注情绪冲突而非单一情绪标签

系统的核心不是简单判断用户开心或难过，而是判断用户在不同模态中表达的情绪是否一致。

这一设计更符合真实人际交流，因为用户的语言内容、声音状态和面部表现并不总是一致。

### 4.3 模态独立分析，减少信息污染

MiMo-V2.5 分别执行：

```text
audio-only
video-only
```

GLM执行：

```text
text-only
```

避免模型根据文字内容反推声音或面部情绪，提高冲突检测的可解释性。

### 4.4 引入输入质量和可靠性机制

系统不会无条件相信所有模态，而是根据输入质量调整其权重。

当人脸不可见、音频过短或ASR质量较低时，系统主动降低判断强度，从而减少误报。

### 4.5 陪伴策略安全、克制

项目将情绪分析结果视为弱证据，不使用“你一定很难过”“你在隐藏情绪”等确定性表达。

数字人以温和确认、倾听和澄清为主，适合作为非医疗型陪伴交互系统。

### 4.6 复用成熟数字人底座

基于 Open-LLM-VTuber v1.2.1，复用其：

- ASR；
- 对话管理；
- TTS；
- Live2D渲染；
- 嘴型同步；
- 表情控制；
- Web/Electron客户端。

团队可以将主要开发资源集中在跨模态情绪冲突检测上。

### 4.7 云API与本地部署可平滑切换

第一版使用 MiMo 和 GLM API，减少本地部署成本。

后续可以在实验室服务器上部署 Qwen Omni 等本地模型，并通过统一 HTTP 接口替换云端 Provider。

### 4.8 可展示、可实验、可扩展

项目既具备可视化演示价值，也保留了实验评测空间，可以进一步研究：

- 不同多模态模型的情绪识别能力；
- 不同冲突融合算法；
- 不同回复策略对用户体验的影响；
- 连续情绪跟踪；
- 个性化陪伴记忆；
- 全双工实时音视频交互。

---

## 5. 项目分工

项目当前由两名成员协作完成。

### 5.1 成员ZZH：仓库、数字人底座、音频与系统集成

成员ZZH为系统主集成人，负责以下内容。

#### 仓库与工程管理

- 创建主项目仓库；
- 固定 Open-LLM-VTuber v1.2.1；
- 设置 Git submodule；
- 建立分支规范；
- 配置 `.gitignore` 和 `.env.example`；
- 定义统一配置文件；
- 维护公共数据结构与接口。

#### 数字人底座

- 配置 Open-LLM-VTuber 环境；
- 跑通 ASR、LLM、TTS 和 Live2D；
- 导入“小蝴蝶”Live2D模型；
- 修改 `model3.json`；
- 注册表情、动作和嘴型参数；
- 配置 `emotionMap`；
- 验证眨眼、嘴型、待机动画和表情。

#### 音频链路

- 获取用户发言开始和结束事件；
- 保存每轮原始音频；
- 将音频统一转换为16 kHz、单声道、PCM WAV；
- 计算音频质量；
- 实现 MiMo audio-only 调用；
- 标准化音频情绪结果；
- 配合 Turn Manager 完成音视频时间对齐。

#### 文本与陪伴回复

- 接入 ASR文本；
- 实现 GLM text-only 情绪分类；
- 实现 GLM 陪伴回复生成；
- 建立安全提示和陪伴策略；
- 将数字人表情和语音风格映射到 Open-LLM-VTuber。

#### 融合与系统集成

- 定义情绪标签空间；
- 实现可靠性计算；
- 实现冲突检测；
- 实现陪伴策略路由；
- 将 Emotion Middleware 接入 Open-LLM-VTuber；
- 完成端到端运行和展示部署。

### 5.2 成员LZY：视频采集与视觉情绪分析

成员LZY负责视频侧完整链路。

#### 摄像头采集

- 使用 OpenCV 读取摄像头；
- 持续采集带时间戳的视频帧；
- 实现环形帧缓冲区；
- 根据 `speech_start_ms` 和 `speech_end_ms` 截取对应视频。

#### 视频预处理

- 视频抽帧；
- 低帧率压缩；
- 控制分辨率和文件体积；
- 输出对应 `turn_id` 的视频文件或帧列表。

#### 人脸与视频质量

- 检测有效人脸；
- 判断多人、遮挡、侧脸、模糊、过暗和过曝；
- 计算有效人脸帧比例；
- 输出视觉输入质量分数。

#### 视觉情绪分析

- 实现 MiMo video-only API 调用；
- 约束模型只依据画面判断；
- 标准化视觉情绪输出；
- 输出细粒度情绪、置信度、质量和证据；
- 实现异常、超时和证据不足处理。

#### 视频侧测试

- 测试摄像头兼容性；
- 测试不同光照和角度；
- 分析视觉误判；
- 提供视觉可靠性阈值建议；
- 配合完成 MiMo 与 Qwen3-Omni-Flash 对照。

### 5.3 共同负责的内容

两名成员共同负责：

- 定义 `TurnRecord`；
- 定义 `ModalityEmotion`；
- 确定情绪标签映射；
- 讨论冲突检测规则；
- 编写和调整提示词；
- 录制测试样本；
- 标注多模态情绪与冲突类别；
- 完成模型对照实验；
- 测试系统延迟、准确率和稳定性；
- 准备最终演示和项目汇报。

### 5.4 分工表

| 工作内容 | 成员ZZH | 成员LZY |
|---|---:|---:|
| 主仓库创建与维护 | 主责 | 评审 |
| Open-LLM-VTuber部署 | 主责 | 了解与测试 |
| Live2D皮套适配 | 主责 | 测试 |
| ASR、TTS和对话链路 | 主责 | — |
| 音频切片与预处理 | 主责 | — |
| 摄像头采集 | 接口定义 | 主责 |
| 视频时间戳缓存 | 协同 | 主责 |
| 视频抽帧与压缩 | 评审 | 主责 |
| MiMo audio-only | 主责 | — |
| MiMo video-only | 接口评审 | 主责 |
| GLM text-only | 主责 | — |
| 统一数据Schema | 主责 | 共同确认 |
| 可靠性与冲突检测 | 主责 | 共同设计 |
| 陪伴策略与回复 | 主责 | 测试 |
| Live2D表情和嘴型 | 主责 | 测试 |
| 测试数据录制 | 共同 | 共同 |
| 对照实验 | 系统集成 | 视觉侧分析 |
| 最终部署与演示 | 主责 | 协助 |

---

## 6. 项目技术栈

### 6.1 核心技术栈

| 模块 | 技术选择 |
|---|---|
| 数字人底座 | Open-LLM-VTuber v1.2.1 |
| 数字人模型 | “小蝴蝶”Live2D皮套 |
| Live2D渲染 | Open-LLM-VTuber Web/Electron前端 |
| Live2D运行格式 | Cubism 3模型文件 |
| 嘴型参数 | `ParamMouthOpenY` |
| 表情控制 | Open-LLM-VTuber `emotionMap` |
| 待机动画 | 原有 Idle `motion3.json` |
| 后端语言 | Python 3.11 |
| 包管理 | uv |
| API服务 | FastAPI |
| 音视频处理 | FFmpeg |
| 摄像头采集 | OpenCV |
| ASR | sherpa-onnx + SenseVoiceSmall |
| 音频情绪 | MiMo-V2.5 API，audio-only |
| 视频情绪 | MiMo-V2.5 API，video-only |
| 文本情绪 | GLM API，text-only |
| 陪伴回复 | GLM API |
| 冲突检测 | Python规则与可靠性加权 |
| TTS | 第一版 Edge TTS |
| 备用多模态模型 | Qwen3-Omni-Flash |
| 数据校验 | Pydantic |
| 测试 | pytest |
| 版本管理 | Git + GitHub/GitLab |
| 配置管理 | YAML + `.env` |
| 日志 | Python logging / JSONL |

### 6.2 第一版固定模型组合

```text
Open-LLM-VTuber v1.2.1
+ 小蝴蝶Live2D
+ SenseVoiceSmall ASR
+ MiMo-V2.5 audio-only emotion
+ MiMo-V2.5 video-only emotion
+ GLM text-only emotion
+ 本地可靠性加权冲突检测
+ GLM陪伴回复
+ Edge TTS
```

### 6.3 备用与后续升级

```text
Qwen3-Omni-Flash
→ 多模态情绪API备用与对照

本地Qwen Omni / 其他Omni模型
→ 后续部署在实验室服务器

CosyVoice / GPT-SoVITS
→ 提升角色语音表现

WebSocket / WebRTC
→ 后续实现连续实时音视频感知

个性化长期记忆
→ 后续提升陪伴连续性
```

### 6.4 推荐工程结构

```text
emotion-companion-avatar/
├── README.md
├── .env.example
├── .gitignore
├── pyproject.toml
├── configs/
│   ├── app.yaml
│   ├── emotion_labels.yaml
│   └── prompts/
├── third_party/
│   └── Open-LLM-VTuber/
├── assets/
│   └── live2d/
├── capture/
│   ├── turn_manager.py
│   ├── audio_capture.py
│   ├── camera_capture.py
│   └── frame_buffer.py
├── emotion_service/
│   ├── schemas.py
│   ├── clients/
│   ├── audio/
│   ├── video/
│   ├── text/
│   ├── fusion/
│   └── policy/
├── companion/
├── integration/
├── tests/
└── runtime/
```

### 6.5 统一数据结构

#### TurnRecord

```json
{
  "turn_id": "turn_0001",
  "speech_start_ms": 1721188801200,
  "speech_end_ms": 1721188805700,
  "audio_path": "runtime/audio/turn_0001.wav",
  "video_path": "runtime/video/turn_0001.mp4",
  "frame_paths": [],
  "transcript": "没事，我今天挺好的。"
}
```

#### ModalityEmotion

```json
{
  "modality": "audio",
  "label": "negative",
  "fine_emotion": "subdued",
  "confidence": 0.74,
  "quality": 0.88,
  "reliability": 0.65,
  "evidence": [
    "语速偏慢",
    "能量较低"
  ]
}
```

#### ConflictResult

```json
{
  "conflict": true,
  "conflict_type": "verbal_positive_behavior_negative",
  "conflict_score": 0.72,
  "strategy": "gentle_check_in",
  "avatar_emotion": "neutral"
}
```

---

## 7. 推荐实施顺序

### 阶段一：数字人底座

- 创建主仓库；
- 固定 Open-LLM-VTuber v1.2.1；
- 跑通默认数字人；
- 配置ASR、TTS和对话模型；
- 导入小蝴蝶皮套；
- 验证嘴型、表情和待机动画。

### 阶段二：音视频按轮次采集

- 定义 `turn_id`；
- 保存每轮音频；
- 建立摄像头时间戳缓存；
- 按同一时间范围导出视频；
- 形成统一 `TurnRecord`。

### 阶段三：三模态情绪分析

- MiMo audio-only；
- MiMo video-only；
- GLM text-only；
- 统一输出格式；
- 增加质量和异常处理。

### 阶段四：冲突检测与陪伴策略

- 实现可靠性；
- 实现冲突类型；
- 实现策略路由；
- 将结果注入陪伴回复模型；
- 映射数字人表情和TTS风格。

### 阶段五：系统集成与评测

- 接入 Open-LLM-VTuber；
- 完成端到端运行；
- 录制测试集；
- 比较单模态和多模态方案；
- 比较 MiMo 和 Qwen；
- 测试延迟、误报率和用户体验。

---

## 8. 预期成果

项目最终预期形成：

1. 一个可运行的AI陪伴型Live2D数字人；
2. 一套按对话轮次同步采集音频和视频的系统；
3. 一个文本、音频、视觉独立情绪分析模块；
4. 一个可靠性加权的跨模态情绪冲突检测模块；
5. 一套安全、克制的陪伴策略；
6. 一套可切换MiMo、Qwen等模型的统一接口；
7. 一组用于评测情绪识别和冲突检测的测试数据；
8. 一个可用于课程展示、竞赛或后续研究扩展的完整原型。
